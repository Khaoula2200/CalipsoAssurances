{% extends 'base.html.twig' %}

{% block title %} Contact
{% endblock %}

{% block body %}

	<div class="container">
	{% for message in app.flashes('success') %}
			<div class="alert alert-success mt-4">
				{{ message }}
			</div>
		{% endfor %}

		{% for message in app.flashes('danger') %}
			<div class="alert alert-danger mt-4">
				{{ message }}
			</div>
		{% endfor %}
		<div class="mt-4 ">
	<h1>Contactez nous</h1>
	<p class="w-50">Lorem ipsum dolor sit amet consectetur adipisicing elit. Quidem, iste veniam! Unde porro repellendus iure! Officia! Lorem ipsum dolor sit amet consectetur, adipisicing elit. A, cumque?</p>
		</div>
		{{form(form)}}
		</div>
	{% endblock %}

